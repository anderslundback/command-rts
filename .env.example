# WebSocket server URL for multiplayer.
# Set this in Vercel project settings as VITE_WS_URL.
# The prefix VITE_ is required for Vite to expose it to the browser bundle.
VITE_WS_URL=wss://your-server.up.railway.app
